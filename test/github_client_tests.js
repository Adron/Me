var assert = require("assert")

describe('Array', function(){
  describe('#indexOf()', function(){
    it('what and how should this show the integration works? How is it testable?', function(){
    	var result = 2 + 2;
    	// this needs to be a real test to verify the github integration works.
      assert.equal(result, 4);
    })
  });
})

